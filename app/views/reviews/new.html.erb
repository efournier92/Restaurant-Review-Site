<h1> Review Form for <%= @restaurant.name %></h1>

<%= form_for [@restaurant, @review] do |f| %>

  <%= f.label :rating %>
  <%= f.text_field :rating %>     <br>

  <%= f.label :body, 'Review' %>
  <%= f.text_area :body %>        <br>

  <%= f.submit 'Add Review' %>

<% end %>
